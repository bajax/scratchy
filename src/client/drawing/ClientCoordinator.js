/**
 * Client-side version of the server coordinator.
 */
const E = require('../../shared/event_types').COORDINATOR;

module.exports = function ClientCoordinator (params, c)
{
	const self = this;
	
}

module.exports.prototype = 
{
	E : E,
}
