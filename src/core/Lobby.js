/**
 * Handles the list of "Canvases" that are available to draw on.
 * Creates new canvases and allows users to join them.
 */
var EventEmitter = require('events').EventEmitter;

module.exports = function Lobby (params, dispatcher)
{
	var canvases = [];



}
