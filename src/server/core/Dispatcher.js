/**
 * Dispatcher handles events that affect the state of the application.  Things like user joins/parts, form submissions when a user updates their profile, etc.
 * We probably only need one per server instance.
 */

module.exports = function Dispatcher (params)
{
	var self = this;

	var name;
	var avatar;
	var level;	

	var presence;


}